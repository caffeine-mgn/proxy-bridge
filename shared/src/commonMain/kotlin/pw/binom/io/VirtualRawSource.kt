package pw.binom.io

/*
import kotlinx.coroutines.runBlocking
import kotlinx.io.Buffer
import kotlinx.io.RawSource

class VirtualRawSource(
    private val virtualInput: VirtualInput,
) : RawSource {

    override fun readAtMostTo(sink: Buffer, byteCount: Long): Long =
        runBlocking {
            virtualInput.readTo(sink, byteCount)
        }

    override fun close() {
        virtualInput.close()
    }
}
*/
